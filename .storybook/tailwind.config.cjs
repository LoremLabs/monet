const colors = require('tailwindcss/colors');

module.exports = {
    theme: {
        extend: {
            colors: {
                theme: colors.lime
            }

        },
    },
    variants: {},
    plugins: [],
};
