# Svelte + TS + Storybook + CustomElements

```bash
npm ci
```

```bash
# Storybook
npm start

# Production build
npm run build

# Dev Builds (watch mode)
# run following to two separate terminals:
npm run build:components -- -w --no-emptyOutDir
npm run build:elements -- -w
```
